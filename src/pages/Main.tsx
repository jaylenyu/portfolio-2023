import React, { FC } from 'react';
import ScrollComponent from '../components/ScrollComponent';

const Main: FC = () => {
  return (
    <div>
      <ScrollComponent />
    </div>
  );
};

export default Main;
