export interface ProjectDataProps {
  id: number;
  name: string;
  img: string;
  title: string;
  date: string;
  skill: string[];
  contents: string[];
}

export interface ShowFadeProps {
  showFade: boolean;
}
